name: Self Hosted Test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Verify runner
        run: |
          echo "Running on self-hosted runner"
          whoami
          uname -a
